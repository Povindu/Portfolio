Extract schema to create sanity types

```terminal
npx sanity schema extract
```

Use above schema to create types (We can use these for the frontend types)

```terminal
npx sanity typegen generate
```
